{
  "name": "k-book",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter web test:e2e",
    "prepare": "pnpm --filter web prepare",
    "start-supabase": "pnpm --filter worker supabase:start",
    "stop-supabase": "pnpm --filter worker supabase:stop",
    "migrate": "pnpm --filter worker supabase:migrate"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "22.12.0"
  },
  "playgroundSupport": "true"
}
